# autotrigger-py-cstrike
python script to help with tricksurf mapping. automatically generates triggers to cover surfaces in counter-strike: source maps. work in progress.. uses hammer++ precise vertices
